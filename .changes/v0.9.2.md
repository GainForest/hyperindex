## v0.9.2
### Fixed
- Fix activity retention cleanup so long-running deployments delete activity log entries older than seven days every hour instead of only at startup.
- Make Tap identity purges transactional so actor records are retained when deleting the actor fails, preventing partially purged identities.
- Remove the unused PostgreSQL whole-document record JSON GIN index, reducing index storage and write maintenance without changing current query behavior.
#### Affects

- operator

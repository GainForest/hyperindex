kind: added
body: Add a generic recordTimeline GraphQL query for creation-time pages across selected collections, using where.collection.in and optional where.did filters with stable cursor pagination. Existing records are backfilled when migrations run, and timeline connections avoid exact total counts.
custom:
  Affects: user

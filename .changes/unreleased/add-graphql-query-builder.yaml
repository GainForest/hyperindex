kind: added
body: Add a Schema Builder panel to GraphiQL so users can select root queries, fill arguments, and check or uncheck schema fields to generate queries directly in the GraphiQL editor.
custom:
  Affects: user

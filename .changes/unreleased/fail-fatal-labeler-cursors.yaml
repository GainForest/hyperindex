kind: changed
body: >-
  Fail health checks and expose diagnostics when an external labeler subscription reaches a non-retryable cursor error, so operators can reset stuck labeler ingestion instead of serving stale label data silently.
custom:
  Affects: operator
